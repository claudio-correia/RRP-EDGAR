Todos as aplicacoes usam o OpenSSL dentro do openssl_source.
É preciso colar lá a versao compactada do .tar.gx
Depois correr ./Configure linux-x86_64
Depois esta pronto para ser usado, nota que os Make files estao a fazer include da versao "openssl-OpenSSL_1_1_1d"


-Usar o git
$ git add -A
$ git commit -m "comment"
$ git push -u origin master
Dentro da pasta DACoSE